---
description: Auto-push changes
---

Whenever a task involving code modifications is completed, automatically sync the changes with the repository without waiting for the user to ask.

// turbo-all
1. Stage all changes: `git add .`
2. Commit changes: `git commit -m "Auto-update from Antigravity"`
3. Push changes: `git push -u origin main`
